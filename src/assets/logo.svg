<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 280 100" fill="none">
  <!-- Power O (the O in OPSYN) -->
  <circle cx="32" cy="32" r="28" stroke="#FF6B00" stroke-width="5" fill="none"/>
  <rect x="29.5" y="8" width="5" height="18" rx="2.5" fill="#FF6B00"/>
  
  <!-- PSYN Text -->
  <text x="72" y="46" font-family="Outfit, sans-serif" font-weight="800" font-size="48" fill="#FFFFFF" letter-spacing="6">PSYN</text>
  
  <!-- Underline (spans OPSYN width only) -->
  <rect x="0" y="64" width="280" height="5" rx="2.5" fill="#FF6B00"/>
  
  <!-- TECHNOLOGIES Text -->
  <text x="0" y="90" font-family="Outfit, sans-serif" font-weight="500" font-size="16" fill="#888888" letter-spacing="4">TECHNOLOGIES</text>
</svg>
